/**
 * Shared types for DECHO.
 *
 * Main responsibility:
 * - Provides small reusable types that do not belong to one feature.
 *
 * Made by: Toon
 */

export type Point = {
  x: number;
  y: number;
};
