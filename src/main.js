import { Game } from './game.js'

/**
 * Entry point — instantiates the Game class.
 * The `new` keyword creates an Object (instance) from the Game Class.
 */
function init() {
    new Game()
}

init();
